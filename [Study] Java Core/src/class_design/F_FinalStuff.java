package class_design;

public class F_FinalStuff {

	/**
	 * MARKING STUFF FINALL
	 * --------------------
	 * 
	 * Usage
	 * - Variable :: prevent it from being changed
	 * - Method   :: prevent it from being overridden, so a certain behavior is guaranteed across the class hierarchy
	 * - Class    :: prevent it from being subclassed
	 * 
	 * Assigning final fields
	 * 	- When declaring them
	 *  - In an instance initializer
	 *  - In the constructor 
	 * 
	 */
}
